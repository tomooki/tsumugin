# TASK-0004: _profile_ranges + _profiles_physical + engine ガード配線

**タスクタイプ**: TDD
**フェーズ**: Phase 2 (engine アダプタ)
**信頼性**: 🔵

## 概要
`engine.py` に `_profile_ranges` (CW=2θ°, TOF=d 換算) と `_profiles_physical` ラッパを追加し、
段階ループの revert ガードに `_cells_physical` と同格で配線。最終 `validity` に profile warnings をマージ。

## 依存
- 前提: TASK-0003
- 後続: TASK-0005

## 完了条件
- [ ] `_profile_ranges(g2hists, radiations, profiles)`: CW は `getdata("x")` の min/max、TOF は
  `d≈(t-Zero)/difC`。取得不能/difC 欠落0 は None (EDGE-003)。
- [ ] `_profiles_physical(g2hists, radiations) -> ValidityReport`: 抽出+レンジ+`check_profile_physicality`。
  抽出不能は passed=True skip。
- [ ] 段階ループ: `if not _cells_physical(g2phases) or not _profiles_physical(g2hists, radiations).passed:
  rwp=gof=inf; converged=False` (engine.py:622 付近)。
- [ ] 最終: `check_profile_physicality` 再評価の warnings/checks を `validity` にマージ (REQ-103)。
- [ ] `radiations` が段階ループ内で参照可能なこと (既存 `radiations` 変数を利用)。

## テスト
- tests/autorietveld/test_profile_ranges.py: モック g2hist (getdata("x")→[10,...,120]) から CW レンジ
  `(10.0,120.0)`; TOF は difC 換算で d レンジ。difC 欠落 → None。
- tests/autorietveld/test_engine_profile_guard.py (`@pytest.mark.gsas` は不要 — 純関数合成でロジック検証):
  `_profiles_physical` を非物理 profiles で False、物理 profiles で True。
- 非回帰: 既存 tests/autorietveld/test_engine*.py が green (物理 instprm で従来挙動)。

## 信頼性
🔵 engine.py:527-661 精読 + getdata("x") 単位 (residual 抽出と同一)。
