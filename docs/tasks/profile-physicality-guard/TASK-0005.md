# TASK-0005: __all__ エクスポート + diagnose 連携回帰

**タスクタイプ**: TDD
**フェーズ**: Phase 2 (仕上げ)
**信頼性**: 🔵

## 概要
`check_profile_physicality` をトップレベル `__all__` へ (昇順)。`hist_profile` が充填されることで
`diagnose_residual` が実プロファイル値を使えることを回帰確認。全ティア green。

## 依存
- 前提: TASK-0004
- 後続: なし

## 完了条件
- [ ] `src/tsumugin/__init__.py` / `autorietveld/__init__.py` に `check_profile_physicality` を昇順追加。
- [ ] `tests/test_public_api.py` 等の `__all__` 昇順・シンボル数テストを更新 (存在すれば)。
- [ ] `diagnose_residual` が `hist_profile` を参照する箇所の回帰テスト (充填後も degrade しない)。
- [ ] `uv run pytest -m "not gsas"` 全 green。

## テスト
- tests/test_exports (既存があれば) にシンボル追加確認
- tests/refine_loop/test_diagnose_residual.py 回帰 (hist_profile 充填で新規 fail なし)

## 信頼性
🔵 既存 __all__ 規約 (CLAUDE.md「トップレベル __all__ は完全昇順」)。
