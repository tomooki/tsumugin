# TASK-0002: check_profile_physicality — TOF 判定 + skip 縮退

**タスクタイプ**: TDD
**フェーズ**: Phase 1 (純関数コア)
**信頼性**: 🔵

## 概要
`check_profile_physicality` に TOF 分岐を追加。ガウス分散 σ² 区間最小 (`_tof_sigma_min_over_range`)・
alpha/beta strict-pos、および欠落/レンジ None の skip 縮退 (EDGE-001/003) を実装。

## 依存
- 前提: TASK-0001
- 後続: TASK-0003

## 完了条件
- [ ] `_tof_sigma_min_over_range(s0,s1,s2,d_lo,d_hi)` が `x=d²` の2次式区間最小を返す (レンジ内頂点考慮)。
- [ ] hard: sig-* いずれか refined のとき σ²<-sign_tol で `passed=False`。alpha/beta-0/beta-1 は refined のとき ≤0 で `passed=False` (strict-pos)。
- [ ] EDGE-001: profiles の要素が空 dict → 当該 hist は skip (warnings に理由、passed に影響しない)。
- [ ] EDGE-003: ranges の要素が None → レンジ依存 (σ²/幅) を skip、符号判定は継続。

## テスト (同ファイルに追加)
- TC-005-01 `sig-0=100,sig-1=5,sig-2=0` → OK / TC-005-E01 `sig-2` 大負で高 d NG / TC-005-B01 端点 σ²≈0 tol OK
- TC-006-01 `alpha=0.5,beta-0=0.02` OK / TC-006-E01 `alpha=0` NG / TC-006-E02 `beta-0=-0.01` NG
- TC-001-E01 空 dict hist → skip (例外なし) / EDGE-003 range None → σ² skip
- TC-NFR-02 同一入力2回で同一結果 (決定論)

## 信頼性
🔵 GSAS TOF プロファイル定義 (σ²=sig0+sig1·d²+sig2·d⁴, 1/alpha,1/beta 発散)。
