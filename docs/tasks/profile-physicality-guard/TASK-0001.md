# TASK-0001: check_profile_physicality — CW 判定 + hard/soft 切り分け

**タスクタイプ**: TDD
**フェーズ**: Phase 1 (純関数コア)
**信頼性**: 🔵

## 概要
`src/tsumugin/autorietveld/validity.py` に `check_profile_physicality` を新設し、CW (X 線/CW 中性子) の
判定を実装する。ガウス幅区間最小 (`_gauss_min_over_range`)・ローレンツ X,Y 非負・SH/L 下限 + soft 上限、
および hard/soft 切り分け (解放フラグ) を含む。

## 依存
- 前提: なし
- 後続: TASK-0002

## 完了条件
- [ ] `check_profile_physicality(*, profiles, radiations, ranges, sign_tol, shl_soft_max, width_floor)` が
  `ValidityReport` を返す (numpy-only, GSAS import なし)。
- [ ] `_gauss_min_over_range(U,V,W,lo_deg,hi_deg)` が端点 + レンジ内頂点で区間最小を返す。
- [ ] hard: U,V,W いずれか refined のとき幅正値性違反で `passed=False`。X,Y,SH/L は各々 refined のとき非負/下限違反で `passed=False`。
- [ ] soft: SH/L > shl_soft_max は `passed` 不変で warnings に追記。
- [ ] REQ-102: 未解放パラメータの符号違反は hard にしない (warning のみ)。

## テスト (tests/autorietveld/test_profile_physicality.py)
- TC-002-01 既定 `U=2,V=-2,W=5` [10°,120°] → OK
- TC-002-E01 `W` 大負 → 低角で NG / TC-002-E02 `U` 負 → NG
- TC-002-B01 頂点最小 ≤0 → NG / TC-002-B02 `U=0` 線形 → 端点判定 / EDGE-103
- TC-003-01 `X=1,Y=0.5` OK / TC-003-E01 `X<0` NG / TC-003-E02 `Y<0` NG / TC-003-B01 `X=-1e-6` tol OK
- TC-004-01 `SH/L=0.002` OK / TC-004-E01 `SH/L=-0.01` NG
- TC-102-01 `X=-0.5` refined=False → OK / TC-102-02 refined=True → NG
- TC-103-01 `SH/L=0.5` → passed=True + warnings / TC-103-02 hard は False にしない

## 信頼性
🔵 GSAS Caglioti/Lorentzian 定義 + interfaces.py 契約。
