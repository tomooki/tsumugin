# TASK-0003: _extract_profile + hist_profile 充填

**タスクタイプ**: TDD
**フェーズ**: Phase 2 (engine アダプタ)
**信頼性**: 🔵

## 概要
`engine.py` に `_extract_profile(g2hists)` を追加し、`Instrument Parameters[0][key]=[default,value,refine]`
から `{key:(value,refined)}` を抽出。`AutoRietveldResult` 構築時に `hist_profile` を key→value に射影して充填。

## 依存
- 前提: TASK-0002
- 後続: TASK-0004

## 完了条件
- [ ] `_extract_profile(g2hists) -> tuple[dict[str,tuple[float,bool]], ...]`。プロファイル関連キーのみ抽出
  (U,V,W,X,Y,SH/L,Zero,sig-0/1/2,alpha,beta-0,beta-1,difC,difA)。
- [ ] 欠落・`Instrument Parameters` 不在 hist → 空 dict (EDGE-001)。
- [ ] `AutoRietveldResult(..., hist_profile=tuple({k:v for k,(v,_) in d.items()} for d in ...))` を配線。

## テスト (tests/autorietveld/test_extract_profile.py — GSAS 非依存モック)
- TC-001-01 モック hist (`data['Instrument Parameters'][0]={'U':[2.0,3.1,True],'V':[-2.0,-2.0,False]}`) →
  `({'U':(3.1,True),'V':(-2.0,False)},)`
- TC-001-02 解放フラグを保持し解放/未解放を区別
- TC-001-E01 `Instrument Parameters` 欠落 → 空 dict、例外なし
- hist_profile 射影が key→value (float) になること

## 信頼性
🔵 GSAS 格納形 (interop/engine 精読)。モックで numpy テスト可能。
