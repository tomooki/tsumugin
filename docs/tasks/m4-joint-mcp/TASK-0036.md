# TASK-0036: model/project 拡張 (TofBankParams + HistogramRef.bank_params + serialization 往復)

**タスクID**: TASK-0036 / **タスクタイプ**: TDD / **推定工数**: 3h
**フェーズ**: Phase 1 / **信頼性レベル**: 🔵 *REQ-001/002/003/404 / interfaces.py TofBankParams / FR-241*

## 関連文書
- [📋 overview.md](overview.md) / requirements REQ-001/002/003/404, EDGE-002 /
  [interfaces.py](../../design/m4-joint-mcp/interfaces.py) / AC: TC-401 系

## タスク概要
`model/project.py` に `TofBankParams` frozen dataclass 新設 (difc: float / difa: float=0.0 /
zero: float=0.0)。`HistogramRef` に `bank_params: TofBankParams | None = None` を**末尾・既定 None で
非破壊追加** (既存 probe/data_ref/instprm_ref/bank_id は不変)。`probe="neutron_cw"|"neutron_tof"` が
既存 xray と共存し、1 Frame に X線+中性子+複数バンク混在の複数 HistogramRef を保持できることを確認。
`store/serialization.py` に `bank_params_to_dict` / `bank_params_from_dict` を追加し、HistogramRef
シリアライズ経路へ配線 (往復対称・欠損は既定値補完)。model/__init__.py で TofBankParams を re-export。

## 依存タスク
- **前提**: なし / **後続**: TASK-0038 (JointHistogram/bank_id 連携)

## 完了条件
- [ ] `HistogramRef(probe="neutron_cw")` / `probe="neutron_tof")` が生成でき既存 xray と共存する 🔵 *TC-401-01*
- [ ] `TofBankParams(difc/difa/zero)` が frozen dataclass で生成でき、既定 difa=0.0/zero=0.0 🔵 *TC-401-02*
- [ ] `HistogramRef.bank_params` が末尾・既定 None で非破壊追加され、既存フィールドが不変 (後方互換) 🔵 *TC-401-02*
- [ ] 1 Frame に X線 + 中性子 (+複数バンク) の複数 HistogramRef を保持できる 🔵 *TC-401-03*
- [ ] `bank_params_to_dict` / `bank_params_from_dict` が往復対称・欠損既定値補完 (単バンク=マルチバンク経路と同一) 🔵 *TC-401-04/EDGE-002*
- [ ] 既存 project/serialization テストが無改変 green (非破壊) 🔵 *REQ-404*

## 実装手順 (TDD)
tdd-red → tdd-green → tdd-refactor → tdd-verify-complete

## 信頼性レベルサマリー
🔵 6/6 — 高品質
