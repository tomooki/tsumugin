# TASK-0038: joint/model + joint/weights (JointHistogram/Model/Metrics/Result + HistogramWeighting)

**タスクID**: TASK-0038 / **タスクタイプ**: TDD / **推定工数**: 5h
**フェーズ**: Phase 2 / **信頼性レベル**: 🔵 *REQ-004/006/008/009/301 / interfaces.py joint/model・joint/weights / FR-242/243*

## 関連文書
- [📋 overview.md](overview.md) / requirements REQ-004/006/008/009/301, NFR-107 /
  [interfaces.py](../../design/m4-joint-mcp/interfaces.py) / AC: TC-402-01/TC-403 系

## タスク概要
`joint/model.py`: `JointHistogram` (two_theta/intensity/probe/weights/hist_weight/bank_id)、
`JointRefinementModel` (phases 共有 + histograms + shared_free_params + per_histogram_free_params で
構造共有/ヒスト独立を分離保持)、`PerHistogramMetrics` (hist_index/probe/rwp/chi2/scale/sigma_source)、
`JointRefinementResult` (aggregate: RefinementResult + per_histogram + warnings)。全 frozen dataclass。
`joint/weights.py`: `HistogramWeighting` (mode="statistical"|"empirical" / empirical_weights)、
`.resolve(model) -> tuple[float,...]` (統計=1.0、経験=empirical_weights 上書き・決定論順)、
`.sigma_source(hist_index) -> "covariance"|"hist_weight"`。engine はまだ実装しない (型と重み解決のみ)。

## 依存タスク
- **前提**: [TASK-0036](TASK-0036.md) / **後続**: [TASK-0039](TASK-0039.md)

## 完了条件
- [ ] `JointRefinementModel` が構造共有 free_params (shared_free_params) とヒスト独立 free_params
  (per_histogram_free_params) を分離保持する 🔵 *TC-402-01*
- [ ] `JointHistogram` / `PerHistogramMetrics` / `JointRefinementResult` が frozen dataclass で生成でき、
  順序 (histograms/per_histogram) が入力順で保存される 🔵 *TC-402-01/REQ-402*
- [ ] `HistogramWeighting` 既定で統計重み (全 1.0) が resolve される 🔵 *TC-403-01*
- [ ] 経験重み (信頼度スカラ) で resolve が上書きされ、ヒスト寄与が変わる 🔵 *TC-403-02/REQ-301*
- [ ] `sigma_source` が σ 由来 (covariance/hist_weight) を返し、PerHistogramMetrics へ明示できる 🔵 *TC-403-03/NFR-107*
- [ ] resolve が決定論順 (ヒスト index 昇順) でビット同一 🔵 *REQ-402*

## 実装手順 (TDD)
tdd-red → tdd-green → tdd-refactor → tdd-verify-complete

## 信頼性レベルサマリー
🔵 6/6 — 高品質
