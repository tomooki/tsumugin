# TASK-0037: model/phase PhaseRef + errors 2 例外 (MCPUnavailableError / MEMUnavailableError)

**タスクID**: TASK-0037 / **タスクタイプ**: TDD / **推定工数**: 3h
**フェーズ**: Phase 1 / **信頼性レベル**: 🔵 *REQ-020/101/102 / interfaces.py PhaseRef・2 例外 / FR-412*

## 関連文書
- [📋 overview.md](overview.md) / requirements REQ-020/101/102 /
  [interfaces.py](../../design/m4-joint-mcp/interfaces.py) / AC: TC-406-07/TC-407-07/TC-407-08

## タスク概要
`model/phase.py` に `PhaseRef` frozen dataclass 新設 (id: str / formula: str | None=None /
element_system: tuple[str,...]=())。既存 `PhaseInstance.phase_ref: str` は不変とし、PhaseRef は
疎結合の別値オブジェクト (PhaseInstance へフィールド追加しない, D6)。`PhaseRef.from_phase_ref(
phase_ref, *, formula=None, element_system=())` 橋渡し staticmethod を実装。
`errors.py` 末尾に `MCPUnavailableError(TsumuginError)` と `MEMUnavailableError(TsumuginError)` を
**非破壊追記** (WebUIUnavailableError と対称)。model/__init__.py で PhaseRef を re-export。

## 依存タスク
- **前提**: なし / **後続**: TASK-0040 (contrast), TASK-0042 (chem base), TASK-0044 (mcp tools)

## 完了条件
- [ ] `PhaseRef` が生成でき (id/formula/element_system)、`PhaseInstance.phase_ref` と id 整合可能 🔵 *TC-406-07*
- [ ] `PhaseRef.from_phase_ref(str, ...)` が既存 str phase_ref から PhaseRef を橋渡し生成する 🔵 *REQ-020*
- [ ] `PhaseInstance` にフィールド追加がなく既存テスト無改変 green (疎結合) 🔵 *REQ-404/D6*
- [ ] `MCPUnavailableError` / `MEMUnavailableError` が `TsumuginError` を基底に errors.py 末尾追加される 🔵 *REQ-101/102*
- [ ] 両例外が `raise` / `except TsumuginError` で捕捉でき、既存 errors テスト無退行 🔵 *TC-407-07/TC-407-08*

## 実装手順 (TDD)
tdd-red → tdd-green → tdd-refactor → tdd-verify-complete

## 信頼性レベルサマリー
🔵 5/5 — 高品質
