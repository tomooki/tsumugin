# TASK-0023: _json.finite_or_none 統合 (Issue #5) ✅ **完了** (TDD開発完了 - 16テストケース全通過 / 全体 446 passed・3 skipped)

**タスクID**: TASK-0023 / **タスクタイプ**: TDD / **推定工数**: 2h
**フェーズ**: Phase 1 / **信頼性レベル**: 🔵 *Issue #5 / 設計 D-Q9*

## 関連文書
- [📋 overview.md](overview.md) / requirements REQ-022 / AC: TC-208-03

## タスク概要
`src/tsumugin/_json.py` に `finite_or_none` (公開名) を新設し、search/tree.py (センチネル判定は
tree 側に残す)・webui/app.py・store/serialization.py (ローカル `_finite_or_none`) の 3 実装を
委譲へ統合する。**挙動不変** — 既存 433 テスト無改変で green。

## 依存タスク
- **前提**: なし / **後続**: なし (独立リファクタ)

## 完了条件
- [x] _json.finite_or_none が単一実装 (inf/NaN/None → None、有限は float) 🔵 — `src/tsumugin/_json.py` 50 行の葉モジュール
- [x] tree.py / webui/app.py / serialization.py が委譲 (tree のセンチネル閾値は tree 内) 🔵 — 3 実装が `_json.finite_or_none` へ委譲、`_EVIDENCE_SENTINEL=1e18` は tree 残置
- [x] 既存全テスト無改変 green + 新規単体テスト (数件) 🔵 — `tests/test_json_util.py` 16 件追加、全体 446 passed / 3 skipped
- [ ] コミットに "Closes #5" 🔵 — コミット時に付与 (本セッションは git commit しない制約のため未実施)

## 実装手順 (TDD)
tdd-red → tdd-green → tdd-refactor → tdd-verify-complete

## 信頼性レベルサマリー
🔵 4/4 — 高品質
