# Issue トリアージ (2026-08-31) — 差分回

対象: 前回 (`2026-08-19-triage.md`) 以降に追加され、**ゲート未付与のまま残っていた 5 件**
(`tomooki/tsumugin`, `main` = c496f2f `Merge pull request #205`。OPEN は 54 件)。

前回と同じ作法で、**本文の主張をコードに当ててから**分類した (確認箇所は各行に明記)。
判定基準 (4 軸) は前回 §1 をそのまま用いる。

> 本回はユーザの明示指示で回したもので、**優先度は提案**である (ラベルは付与済み = 下表 §4)。
> なお当時 `docs/design/issue-auto-loop/architecture.md` §9 は「トリアージそのものは自動化しない」と
> 宣言していたが、2026-09-03 に一次トリアージのルーティンが稼働したため**同宣言は撤回済み**
> (§9.1)。`priority:*` (本書) が正で、ルーティンの `prio:*` は一次見立てである。

---

## 1. 結論 — 5 件は独立した 5 件ではなく、1 本の系統である

#208 / #209 / #210 / #211 は**同一の実解析セッションから出た 1 つの病理の 4 つの断面**で、
順に並べると因果がそのまま鎖になる。個別に直すと順序を誤る。

```
#211 少数相の ADP を凍結する語彙が無い  (free_uiso_labels=() が「全解放」)
   │        ↓ 少数相 Uiso が発散 (10^8–10^9 Å²)
   ├──> #209 Debye-Waller ≈ 0 で Bragg 強度を出さないのに wt = 0.14 を返す「幽霊相」
   │        ↓ その解が候補として残る
   └──> #210 相集合選択が validity を見ないので、その非物理解が ΔBIC 10^4 で勝つ

#208 スケールを初期化/固定/系列共有できない
        → 占有率とスケールの縮退 (r ≈ −0.9) を破れない = 上の発散を招く側の半分
```

**#211 が根**で、しかも**既存の #189 (P0) と同一のコード行**である (下記 §2)。
したがって着手順は **#189+#211 → #210 → #209 → #208 (+#202)**。
#209 を先に作っても「幽霊を検出する計器」が増えるだけで、幽霊は出続ける。

---

## 2. 実地確認

### #211 — `free_uiso_labels=()` が全原子解放 / ADP 凍結の語彙が無い

| 主張 | 確認 | 判定 |
|---|---|---|
| `info.get("uiso_labels") or info["labels"]` で空タプルが全原子に落ちる | `autorietveld/engine.py:850` に現存。書き込み側は `:730` `list(spec.free_uiso_labels)` | **本文どおり** |
| `frozen_coord_labels` があるので非対称 | `model.py:288` に存在 (`free_uiso_labels` は `:285`、既定 `()`) | **本文どおり** |
| `("__frozen__",)` で実質凍結できる (hack) | 未実行 (gated)。ただし報告者が検証済と明記 | 保留 |

⚠ **#211 は #189 と同じ 1 行の欠陥**である (#189: 「`free_uiso_labels=()` が『解放しない』でなく
全原子にフォールバックする」, `bug` / P0 / `gate:gsas`)。両者は重複だが**捨てる側が無い**:

- #189 は「壊れている」ことを言い、前回トリアージ §2 が
  「素直に `is None` へ変えると**全レシピで Uiso が一切解放されない**逆向きの沈黙誤りになる。
  『未指定』と『明示的に空』を**型で分ける**必要がある」と警告して止まっている。
- #211 はまさにその型設計を 3 案 (`frozen_uiso_labels` 追加 / `None`=全解放と `()`=凍結 /
  最低限の警告) で提示しており、**#189 が欠いていた出口**にあたる。
- さらに #211 だけが持つ追加要求が 2 つある — `RefinementStage` の `uiso` フラグを**相ごとに**
  出し分ける (現状は全相一律)、および現行 hack の記録。

→ **クローズせず 1 PR で扱う**。#211 を #189 と同じ P0 に置く (§4)。

### #210 — 相集合選択が validity を参照していない

| 主張 | 確認 | 判定 |
|---|---|---|
| 選択は区間総 BIC だけで決まる | `insitu/anchor/select.py:37 frame_bic` / `:103 select_crossover`。`validity_passed` の参照は**無い** (`grep validity_passed src/tsumugin/insitu/` の hit は extract/segment/engine/model のみ) | **本文どおり** |
| `ValidityReport` は正しく `False` にしている | `segment.py:88` / `extract.py:147` が `res.validity.passed` を `FrameRietveldResult.validity_passed` (`insitu/model.py:490`) へ載せている = **検出はできていて選択が使っていない** | **本文どおり** |
| `crossovers` に validity が残らない | `Crossover` (`anchor/model.py`) は `crossover_frame / total_bic / onset_frame / monotonic / reason / bond_gate` の 6 つ。validity 由来の欄は無い | **本文どおり** |
| 罰則調整では届かない (`per_phase_params` 既定 12 に対し必要値 ~950) | 既定は `AnchorConfig.per_phase_params: int = 12` を確認。必要値 950 は報告者の実測 (未追試) | 既定値のみ確認 |

**修正の型は既に隣に在る** — `AnchorConfig.require_bond_validity` (既定 `False`) が
`select_crossover` の中で候補をふるい、結果を `Crossover.bond_gate` に残す構造
(`select.py:159-170`, `_bond_ok_frame:69`)。同じ形で `validity_passed` のゲートを 1 本足せばよい。
**新しい機構を設計する必要が無い**のがこの件を安くしている。

なお `require_anchor_validity` (既定 `False`) は**アンカー抽出**にしか効かない (`extract.py:209`)
ので、これを有効化しても本件は直らない。

### #209 — 重量分率が計算強度に裏付けられているか検査していない

| 主張 | 確認 | 判定 |
|---|---|---|
| 相ごとの計算強度シェアが結果に無い | `Reflection Lists` を読んでいるのは `mem/gsas.py` と `workbench/curves.py` だけ。`autorietveld` 側に `Fcsq`/`Icorr` の集計は**存在しない** | **本文どおり** |
| 重量分率は GSAS 由来 | `engine.py:_weight_fraction_maps` が `calcMassFracs` を呼び、esd の 3 状態まで区別している = **wt 側は既に丁寧**。突き合わせる相手だけが無い | **本文どおり** |
| `check_phase_set` の単調性は報告されるだけ | 既知 (M8-③ 設計どおり。選択を覆さないのは仕様) | **本文どおり** |

この件は前回 §2 の #192 (「除外窓で報告 Rwp が系統的に過小になる」) と**同じクラス** —
*報告する数字が名乗っている量と一致していない*。#192 が P1 なので本件も P1 に揃える。

### #208 — ヒストグラムスケールを初期化・固定・系列共有する手段が無い

| 主張 | 確認 | 判定 |
|---|---|---|
| `run_auto_rietveld` の初期値引数は 5 つで scale が無い | `engine.py:2154-2160` に `initial_cell_scale` / `initial_cells` / `initial_fractions` / `initial_occupancies` / `initial_coord_jitter`。**`initial_scale` は無い** | **本文どおり** |
| 結果にスケールと esd が載らない | `AutoRietveldResult` は `hist_absorption` / `hist_profile` を持つがスケール欄は無い | **本文どおり** |
| 系列で warm start されるのはセルと分率だけ | `insitu/_warmstart.py:68` (#202 の引用と一致) | **本文どおり** |

要求は 4 本 (`initial_scale` / 固定手段 / 系列共有 / 結果露出) + 診断 1 本 (occ–scale 相関警告)。
`HistogramSpec` と結果 dataclass に手が入るので、前回 §2 が #194 を
「設計変更を伴うので P0 の小修正とは分ける」として P1 に置いたのと同じ扱いにする。

**#202 と重なる** — #202 の要求 2 (`initial_occupancies` が系列/アンカーから到達できない) と
#208 の要求 3 (系列でのスケール共有) は、どちらも
「runner のシグネチャに何を通すか」という同一の設計判断に乗る。**先に #202 で方針を決める**こと。

### #202 — フレーム間で構造パラメータを運ぶ手段が無い (ゲートのみ付与済だった)

`gate:gsas` は付いていたが `priority:*` / `area:*` が欠けていた (前回の付与漏れ)。
本文は自身で「既定の変更を求めるものではなく、**意図の明示か / 到達経路の追加か /
非到達理由の宣言か**のいずれか」と述べており、これは CLAUDE.md の
★ 不変条件「露出しないと決めた場合は理由を明示的に宣言する」そのもの。→ P1 / `area:operando`。

---

## 3. 本文とコードの食い違い

**今回は 0 件** (前回は 53 件中 5 件)。4 件とも実地確認で本文の記述が裏付けられた。
唯一の注意は **#211 と #189 の重複**で、これは食い違いではなく**同一欠陥の 2 枚のチケット**である。

---

## 4. 適用したラベル

| # | priority | area | gate | 根拠 |
|---|---|---|---|---|
| #211 | **P0** | autorietveld | gsas | #189 (P0) と同一行の欠陥。#189 が欠いていた型設計の出口を持つ |
| #210 | **P0** | operando | gsas | operando の主結果 (相集合) が非物理解に決まる。検出は既にあり、修正の型 (`bond_gate`) も隣に在る |
| #209 | P1 | autorietveld | gsas | 報告する量が実体と一致しない (#192 と同クラス)。#211/#210 の下流の計器 |
| #208 | P1 | autorietveld | gsas | 回避策の API 化 + dataclass 設計変更 (#194 と同扱い)。#202 の方針決めが先 |
| #202 | P1 | operando | (既存) | 「宣言なき非到達」。最小なら docstring 1 段落で閉じる |

`gate:gsas` を 4 件すべてに置いた理由: 変更対象はいずれも実精密化の結果に効き、
受け入れには gated な anchor / autorietveld 統合テストが要る。
**クラウド (`gate:fast`) へ回さない** — runner に GSAS-II が無く「fast tier が green だから完了」と
報告する周回になる (`architecture.md` §5.2)。

> #210 を opt-in (既定 `False` のゲート) として実装するなら、ロジック自体は `select.py` が
> numpy/dataclass のみなので fast tier で証明できる。それでも**既定の選択挙動に触れる**以上、
> 受け入れは gated な anchor 統合テストまで見る。

これで **OPEN 54 件すべてが `gate:*` と `priority:*` を持つ** (検証: 未付与 0 件)。

## 5. ループへの投入は行っていない

4 件はすべて `gate:gsas` = **ローカル専用**であり、導入順序 (`architecture.md` §7) は
まだ「段 1: fast 自動 PR (実装済・実走待ち)」の位置にある。`loop:queued` は付けていない
(現在 queued は #105 / #119 / #127 の 3 件のまま)。
