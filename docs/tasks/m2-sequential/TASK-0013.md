# TASK-0013: PersistentLedger — JSONL 追記 + 再オープン検証

**タスクID**: TASK-0013 / **タスクタイプ**: TDD / **推定工数**: 3h
**フェーズ**: Phase 1 - モデル/永続化基盤 / **信頼性レベル**: 🔵 *REQ-010/401, NFR-105/203, 設計 D4*

## 関連文書
- [📋 overview.md](overview.md) / requirements REQ-010/012/401, EDGE-003 / AC: TC-106-01/02/04/05/07

## タスク概要
`store/persistent.py` に `PersistentLedger(path)` を実装。Ledger と同一契約 (append/entries/verify)。
オープン時に既存 JSONL を読込み全チェーン検証 (破損で `LedgerIntegrityError` 新設、無修復)。
append は 1 行追記 ("a" モードのみ)。ハッシュ計算は ledger.py の既存関数を共有 (必要なら
`_canonical_json`/`_compute_hash` を公開ヘルパへ昇格、D-Q5)。

## 依存タスク
- **前提**: なし / **後続**: TASK-0014, 0019

## 完了条件
- [ ] append → 再オープン → verify() True + エントリ全量一致 🔵 *TC-106-01*
- [ ] 再オープン後の append でチェーン連結 (index 連番継続) 🔵 *TC-106-02*
- [ ] 1 行改竄 → LedgerIntegrityError + ファイル無変更 🔵 *TC-106-04/EDGE-003*
- [ ] 削除・上書き API 不在 + 追記のみ (先頭バイト列不変) 🔵 *TC-106-05/07*
- [ ] in-memory Ledger と同一 (kind,payload) 列で同一 hash 列 (決定論) 🔵

## 主要実装項目
- `src/tsumugin/store/persistent.py`, `src/tsumugin/errors.py` (LedgerIntegrityError),
  `src/tsumugin/store/ledger.py` (ヘルパ公開昇格のみ・挙動不変)
- テスト: `tests/test_persistent_store.py` (tmp_path)

## 実装手順 (TDD)
tdd-red → tdd-green → tdd-refactor → tdd-verify-complete

## 信頼性レベルサマリー
🔵 5/5 — 高品質
