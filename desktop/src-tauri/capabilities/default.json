{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Tsumugin Workbench window. Minimal core permissions only — the window is a plain WebView onto the FastAPI-served UI; no native IPC commands are exposed (see src-tauri/src/lib.rs).",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
